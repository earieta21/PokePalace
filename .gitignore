# env
.env
backend/.env

# node
node_modules
backend/node_modules
dist

# macOS
.DS_Store
**/.DS_Store

# Local Netlify folder
.netlify

# Salida de trabajo/marketing generada localmente (fotos, videos, drafts) —
# nunca debe versionarse, infla el repo de forma permanente.
output/
